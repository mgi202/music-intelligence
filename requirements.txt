# Music Intelligence System — Stage 0 dependencies
# All versions pinned. Run: pip install -r requirements.txt

# YouTube Music
ytmusicapi==1.12.1

# Metadata enrichment
musicbrainzngs==0.7.1
requests==2.32.3
rapidfuzz==3.9.7

# Config
python-dotenv==1.0.1

# Discogs (unofficial client wraps the REST API)
discogs-client==2.3.0

# Utilities
click==8.1.7          # CLI scripts
tabulate==0.9.0       # Table output in CLI
tqdm==4.66.5          # Progress bars for long ingestion runs

# API + frontend
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.28.1         # required by fastapi.testclient.TestClient (used in tests)
